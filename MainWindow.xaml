<Window x:Class="ModernModTranslator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tradutor de Mods Universal - Versão BR" Height="650" Width="900"
        WindowStyle="None" ResizeMode="NoResize" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <SolidColorBrush x:Key="BgColor" Color="#1E1E24"/>
        <SolidColorBrush x:Key="PanelColor" Color="#2B2B36"/>
        <SolidColorBrush x:Key="AccentColor" Color="#6C63FF"/>
        <SolidColorBrush x:Key="AccentHoverColor" Color="#837DFF"/>
        <SolidColorBrush x:Key="TextColor" Color="#EEEEEE"/>
        <SolidColorBrush x:Key="TextDimColor" Color="#AAAAAA"/>

        <Style TargetType="Button" x:Key="ModernBtn">
            <Setter Property="Background" Value="{StaticResource PanelColor}"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="8" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3E3E4A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="ActionBtn" BasedOn="{StaticResource ModernBtn}">
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BgColor}" CornerRadius="20" Padding="25">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="10" Opacity="0.6" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/> <RowDefinition Height="80"/> <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="70"/>   </Grid.RowDefinitions>

            <Grid Grid.Row="0" MouseDown="TopBar_MouseDown" Background="Transparent">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="TRADUTOR" Foreground="White" FontWeight="Bold" FontSize="18"/>
                    <TextBlock Text=" DE MODS BR" Foreground="{StaticResource AccentColor}" FontWeight="Bold" FontSize="18" Margin="5,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="—" Width="40" Height="30" Click="BtnMinimize_Click" Style="{StaticResource ModernBtn}" Background="Transparent"/>
                    <Button Content="✕" Width="40" Height="30" Click="BtnClose_Click" Style="{StaticResource ModernBtn}" Background="Transparent" Foreground="#FF5555"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Margin="0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="MODO DE OPERAÇÃO" Foreground="{StaticResource TextDimColor}" FontSize="11" FontWeight="Bold" Margin="0,0,0,5"/>
                    <ComboBox x:Name="cmbMode" SelectionChanged="CmbMode_SelectionChanged" Height="40" VerticalContentAlignment="Center">
                        <ComboBoxItem Content="Arquivo de Mod Único (.jar)" IsSelected="True"/>
                        <ComboBoxItem Content="Pacote de Mods (Pasta)"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="IDIOMA DE DESTINO" Foreground="{StaticResource TextDimColor}" FontSize="11" FontWeight="Bold" Margin="0,0,0,5"/>
                    <ComboBox x:Name="cmbLanguage" SelectedValuePath="Tag" Height="40" VerticalContentAlignment="Center">
                        <ComboBoxItem Content="Português (Brasil)" Tag="pt" IsSelected="True"/>
                        <ComboBoxItem Content="Inglês (English)" Tag="en"/>
                    </ComboBox>
                </StackPanel>
            </Grid>

            <StackPanel Grid.Row="2" Margin="0,0,0,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="180"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="btnSec" Content="SELECIONAR MOD" Click="BtnSec_Click" Style="{StaticResource ModernBtn}" Height="45"/>
                    <TextBlock x:Name="txtYol" Grid.Column="1" Text="Nenhum arquivo selecionado..." VerticalAlignment="Center" Margin="20,0,0,0" Foreground="{StaticResource TextDimColor}" FontStyle="Italic" TextTrimming="CharacterEllipsis"/>
                </Grid>
                <TextBlock x:Name="txtInfo" Text="" Foreground="{StaticResource AccentColor}" Margin="0,5,0,0" HorizontalAlignment="Right" FontWeight="Bold"/>
            </StackPanel>

            <Border Grid.Row="3" Background="{StaticResource PanelColor}" CornerRadius="12" Padding="10" Margin="0,0,0,15">
                <ScrollViewer x:Name="scrlLog" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="txtLog" Text="Pronto para começar..." Foreground="{StaticResource TextDimColor}" FontFamily="Consolas" TextWrapping="Wrap" FontSize="13"/>
                </ScrollViewer>
            </Border>

            <StackPanel x:Name="pnlTotalProgress" Grid.Row="4" Margin="0,0,0,10" Visibility="Collapsed">
                <Grid Margin="0,0,0,5">
                    <TextBlock Text="PROGRESSO GERAL" Foreground="White" FontSize="10" FontWeight="Bold"/>
                    <TextBlock x:Name="txtTotalYuzde" Text="0 / 0" HorizontalAlignment="Right" Foreground="White" FontSize="10" FontWeight="Bold"/>
                </Grid>
                <ProgressBar x:Name="progTotal" Height="10" Value="0" Maximum="100" Foreground="{StaticResource AccentColor}"/>
            </StackPanel>

            <StackPanel Grid.Row="5" Margin="0,0,0,5">
                <Grid Margin="0,0,0,5">
                    <TextBlock x:Name="txtCurrentStatus" Text="Status da Tradução" Foreground="{StaticResource TextDimColor}" FontSize="11"/>
                    <TextBlock x:Name="txtCurrentYuzde" Text="%0" HorizontalAlignment="Right" Foreground="{StaticResource TextDimColor}" FontSize="11"/>
                </Grid>
                <ProgressBar x:Name="progCurrent" Height="6" Value="0" Maximum="100" Opacity="0.8"/>
            </StackPanel>

            <Button x:Name="btnBaslat" Grid.Row="6" Content="INICIAR TRADUÇÃO COM IA" Click="BtnBaslat_Click" Style="{StaticResource ActionBtn}" Margin="0,10,0,0" Height="50" IsEnabled="False"/>
        </Grid>
    </Border>
</Window>
